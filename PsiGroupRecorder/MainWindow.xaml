<Window x:Class="PsiGroupRecorder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PsiGroupRecorder"
        mc:Ignorable="d"
        Title="PsiGroupRecorder" Height="450" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="{Binding Path=Visu0.SkeletonCount}" FontSize="48" FontWeight="Bold"/>
        <Label Grid.Row="0" Grid.Column="1" Content="{Binding Path=Visu1.SkeletonCount}" FontSize="48" FontWeight="Bold"/>
        <Image Grid.Row="1" Grid.Column="0" Source="{Binding Path=Visu0.Image}" />
        <Image Grid.Row="1" Grid.Column="1" Source="{Binding Path=Visu1.Image}" />
    </Grid>
</Window>
